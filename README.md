# EasyFit — Документация проекта

## Оглавление

- [Общий обзор](#общий-обзор)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Запуск проекта](#запуск-проекта)
- [Мобильное приложение (React Native)](#мобильное-приложение-react-native)
  - [Навигация](#навигация)
  - [Экраны](#экраны)
  - [Компоненты](#компоненты)
  - [Данные и хранение](#данные-и-хранение)
  - [Тема и стили](#тема-и-стили)
  - [Аутентификация](#аутентификация)
- [Backend (Node.js)](#backend-nodejs)
  - [Структура бэкенда](#структура-бэкенда)
  - [API эндпоинты](#api-эндпоинты)
  - [База данных PostgreSQL](#база-данных-postgresql)
  - [Middleware](#middleware)
- [Landing Page](#landing-page)
---
## Общий обзор

**EasyFit** — мобильное фитнес-приложение на React Native (Expo), позволяющее:

- Просматривать **каталог из 20 упражнений** с пошаговыми инструкциями
- **Создавать тренировки** из упражнений, настраивая подходы/повторения/отдых
- **Выполнять тренировки** с таймером отдыха и логированием подходов
- **Планировать** тренировки в календаре по дням
- Отслеживать **прогресс** — кольцо прогресса, серия дней, распределение по мышцам
- **Регистрироваться** и вести профиль с данными тела (рост, вес, ИМТ) в разработке

Весь интерфейс на **русском языке**, дизайн в стиле **glassmorphism** (тёмная тема, полупрозрачные карточки с blur).

---

## Технологический стек

| Слой | Технологии |
|---|---|
| **Мобильное приложение** | React Native 0.81, Expo SDK 54, React 19.1, TypeScript 5.9 |
| **Навигация** | expo-router v6 (файловая маршрутизация) |
| **Анимации** | react-native-reanimated 4.1 |
| **UI** | expo-blur, react-native-svg, expo-haptics |
| **Хранение (клиент)** | @react-native-async-storage/async-storage |
| **Backend** | Node.js, Express 4.21 |
| **База данных** | PostgreSQL (через `pg` 8.13) |
| **Аутентификация** | JWT (jsonwebtoken 9.0) + bcrypt 5.1 |
| **Landing** | Чистый HTML/CSS/JS |

---
## Структура проекта

```
Fit-app/
├── app/                          # Экраны приложения (Expo Router)
│   ├── _layout.tsx               # Корневой layout (AuthProvider + Stack)
│   ├── (tabs)/                   # Вкладки нижней навигации
│   │   ├── _layout.tsx           # Tab Navigator (5 вкладок)
│   │   ├── index.tsx             # Главная (дашборд, прогресс)
│   │   ├── exercises.tsx         # Каталог упражнений
│   │   ├── explore.tsx           # Мои тренировки
│   │   ├── calendar.tsx          # Календарь
│   │   └── profile.tsx           # Профиль / Вход
│   ├── exercise/
│   │   └── [id].tsx              # Детали упражнения
│   └── workout/
│       ├── [id].tsx              # Детали тренировки
│       ├── create.tsx            # Создание тренировки (модалка)
│       └── run.tsx               # Выполнение тренировки
│
├── components/                   # Переиспользуемые компоненты
│   ├── animated-components.tsx   # 8 анимированных компонентов
│   ├── glass-card.tsx            # Glassmorphism карточка
│   ├── icons.tsx                 # 30+ SVG иконок
│   ├── exercise-icons.tsx        # 20 иконок упражнений
│   ├── badge.tsx                 # Бейджи (мышцы, оборудование, сложность)
│   ├── button.tsx                # Кнопка (4 варианта)
│   ├── chip-filter.tsx           # Фильтр-чипы (горизонтальный скролл)
│   ├── exercise-card.tsx         # Карточка упражнения
│   ├── search-bar.tsx            # Поисковая строка
│   ├── intro-screen.tsx          # Анимированный сплеш-экран
│   ├── themed-text.tsx           # Текст с темой
│   └── themed-view.tsx           # View с темой
│
├── constants/
│   └── theme.ts                  # Цвета, отступы, радиусы, шрифты
│
├── data/
│   ├── types.ts                  # TypeScript типы и лейблы
│   ├── exercises.ts              # Каталог 20 упражнений (статический)
│   ├── storage.ts                # AsyncStorage CRUD операции
│   ├── api.ts                    # REST API клиент
│   └── auth-context.tsx          # React Context аутентификации
│
├── hooks/
│   ├── use-color-scheme.ts       # Определение тёмной/светлой темы
│   └── use-theme-color.ts        # Получение цвета из темы
│
├── backend/                      # Node.js бэкенд
│   ├── package.json
│   ├── .env                      # Конфигурация (порты, пароли, JWT)
│   └── src/
│       ├── index.js              # Express сервер (точка входа)
│       ├── db/
│       │   ├── pool.js           # Пул подключений PostgreSQL
│       │   ├── create.js         # Создание таблиц БД
│       │   └── seed.js           # Заполнение справочника упражнений
│       ├── middleware/
│       │   └── auth.js           # JWT middleware
│       └── routes/
│           ├── auth.js           # Авторизация и профиль
│           ├── workouts.js       # Тренировки, логи, расписание, стата
│           └── exercises.js      # Каталог упражнений из БД
│
├── landing/                      # Лендинг страница
│   ├── index.html                # HTML разметка
│   ├── style.css                 # Стили
│   └── script.js                 # Логика демо
│
├── start.bat                     # Запуск всех сервисов (Windows)
├── app.json                      # Конфигурация Expo
├── package.json                  # Зависимости приложения
└── tsconfig.json                 # TypeScript конфигурация
```
---
## Запуск проекта

### Быстрый старт (Windows)

Двойной клик по `start.bat` — скрипт проверит все зависимости и запустит:

| Сервис | Порт | Описание |
|---|---|---|
| Backend API | `3000` | Express + PostgreSQL |
| Landing Page | `5500` | Статический сайт (serve) |
| Expo Dev Server | `8081` | React Native сборка |
### Ручной запуск

```bash
# 1. База данных (один раз)
psql -U postgres -p 4000 -c "CREATE DATABASE fitapp;"
затем развренуть дамп
# 2. Backend
cd backend && npm start

# 3. Приложение
cd Fit-app && npm install && npx expo start --go
```
### Конфигурация (.env)

```env
DB_HOST=localhost
DB_PORT=4000        # Порт PostgreSQL
DB_NAME=fitapp
DB_USER=postgres
DB_PASSWORD=1
JWT_SECRET=fitapp_super_secret_key_change_me_in_production
JWT_EXPIRES_IN=30d
PORT=3000           # Порт Express
```
---
## Мобильное приложение (React Native)

### Навигация

Используется **файловая маршрутизация** Expo Router v6. Каждый файл в `app/` — это экран.

```
Корень (_layout.tsx)
 └─ AuthProvider
    └─ ThemeProvider
       └─ Stack Navigator
          ├─ (tabs)/            ← Tab Navigator (5 вкладок)
          │   ├─ index          ← Главная
          │   ├─ exercises      ← Каталог
          │   ├─ explore        ← Мои тренировки
          │   ├─ calendar       ← Календарь
          │   └─ profile        ← Профиль
          │
          ├─ exercise/[id]      ← Детали упражнения (slide)
          ├─ workout/[id]       ← Детали тренировки (card)
          ├─ workout/create     ← Создание тренировки (modal)
          └─ workout/run        ← Выполнение тренировки (fullScreenModal)
```

**Tab Bar** — кастомный плавающий бар (pill-shaped), позиция `absolute`, `borderRadius: 25`, с haptic feedback на iOS.

---

### Экраны

#### Главная (`index.tsx`)

Дашборд с виджетами:

- **Приветствие** — зависит от времени суток (Доброе утро / День / Вечер / Ночь)
- **Кольцо прогресса** — визуальный процент дня (завершённые тренировки / запланированные)
- **Статистика недели** — количество тренировок, суммарное время, серия дней подряд
- **Недельная активность** — бар-чарт (Пн—Вс), подсвечивается текущий день
- **Распределение по мышцам** — горизонтальные бары с процентами задействованных групп
- **Сегодняшние тренировки** — карточки запланированных тренировок с кнопкой «Начать»
- **Последние тренировки** — журнал выполненных с длительностью

Все данные загружаются из **локального AsyncStorage**.

#### Каталог упражнений (`exercises.tsx`)

- Поисковая строка с анимированной рамкой
- Горизонтальные чипы для фильтрации по группам мышц
- FlatList карточек упражнений
- Нажатие → переход к `exercise/[id]`

#### Мои тренировки (`explore.tsx`)

- Список созданных тренировок (GlassCard)
- Для каждой: количество упражнений, кнопки «Начать» и «Удалить»
- FAB-кнопка «Создать тренировку» → `workout/create`

#### Календарь (`calendar.tsx`)

- Навигация по месяцам (← текущий →)
- Сетка дней с точками-индикаторами:
  - 🟣 Фиолетовая — запланирована тренировка
  - 🟢 Зелёная — выполнена тренировка
- Планирование тренировки на выбранный день (модальный picker)
- Просмотр выполненных тренировок за день
- Статистика месяца (тренировок / минут)

#### Профиль (`profile.tsx`)

Два режима:

**Не авторизован:** Форма входа и регистрации
- Переключатель «Вход / Регистрация»
- Поля: email, пароль, имя (при регистрации)
- Кнопка показать/скрыть пароль (иконка глаза)

**Авторизован:** Профиль пользователя
- Аватар (первая буква имени) + имя + email + цель
- Статистика: всего тренировок, суммарные минуты, тренировок на этой неделе
- Параметры тела: рост, вес, расчёт ИМТ
- Редактирование профиля (inline)
- Смена пароля
- Кнопка «Выйти»

#### Детали упражнения (`exercise/[id].tsx`)

- Большая SVG-иконка упражнения с shimmer-эффектом
- Бейджи: группы мышц, оборудование, сложность
- Описание
- Пронумерованные шаги выполнения

#### Детали тренировки (`workout/[id].tsx`)

- Название тренировки, количество упражнений
- Суммарно подходов/повторений
- Пульсирующая кнопка «Начать тренировку»
- Список упражнений с иконками и бейджами

#### Создание тренировки (`workout/create.tsx`)

Модальное окно с двумя экранами:

1. **Основная форма** — название + выбранные упражнения со stepper'ами (подходы, повторения, отдых)
2. **Picker упражнений** — поиск + список с чекбоксами

Сохраняется в **локальный AsyncStorage**.

#### Выполнение тренировки (`workout/run.tsx`)

Три фазы:

1. **Упражнение** — текущее упражнение, прогресс-бар, точки подходов, кнопки «Завершить подход» / «Пропустить»
2. **Отдых** — обратный таймер с вибрацией по окончании, кнопка «Пропустить отдых»
3. **Завершено** — сводка (время, упражнения, завершённые подходы), кнопка «Сохранить и выйти»

Результат сохраняется как `WorkoutLog`.

---

### Компоненты

#### Анимированные (`animated-components.tsx`)

| Компонент | Описание |
|---|---|
| `ScalePressable` | Кнопка с пружинным уменьшением при нажатии |
| `FadeInView` | Появление с плавным смещением (вверх/вниз/влево/вправо) |
| `StaggeredItem` | FadeIn с задержкой по индексу (для списков) |
| `PulseView` | Пульсирующее свечение (для кнопок «Начать») |
| `AnimatedCounter` | Анимированное число (плавный счётчик) |
| `ProgressRing` | SVG-кольцо прогресса с анимацией заполнения |
| `ShimmerGlow` | Мерцающее свечение (для иконок упражнений) |
| `AnimatedTabIcon` | Пружинное увеличение для активной вкладки |

#### UI-компоненты

| Компонент | Файл | Описание |
|---|---|---|
| `GlassCard` | `glass-card.tsx` | Glassmorphism карточка (blur + полупрозрачный фон) |
| `Badge` | `badge.tsx` | Текстовый бейдж (4 варианта: default / tint / success / danger) |
| `MuscleGroupBadge` | `badge.tsx` | Бейдж группы мышц (русский лейбл) |
| `EquipmentBadge` | `badge.tsx` | Бейдж оборудования |
| `DifficultyBadge` | `badge.tsx` | Бейдж сложности (цвет по уровню) |
| `Button` | `button.tsx` | Кнопка (primary / secondary / danger / ghost), поддержка иконки и loading |
| `ChipFilter` | `chip-filter.tsx` | Горизонтальная прокрутка фильтр-чипов |
| `ExerciseCard` | `exercise-card.tsx` | Карточка упражнения для каталога |
| `SearchBar` | `search-bar.tsx` | Поисковая строка с анимацией фокуса |
| `IntroScreen` | `intro-screen.tsx` | Сплеш-экран с приветствием и мотивационной цитатой |

#### Иконки

- **`icons.tsx`** — 30+ общих SVG-иконок (Dumbbell, Search, Plus, Trash, Chevron, User, Eye, Lock и т.д.)
- **`exercise-icons.tsx`** — 20 уникальных SVG-иконок для каждого упражнения + маппинг `EXERCISE_ICON_MAP`

---

### Данные и хранение

#### Типы (`data/types.ts`)

```typescript
type MuscleGroup = 'chest' | 'back' | 'shoulders' | 'biceps' | 'triceps' |
                   'legs' | 'glutes' | 'abs' | 'forearms' | 'calves';

type Equipment = 'barbell' | 'dumbbell' | 'cable' | 'machine' |
                 'bodyweight' | 'kettlebell' | 'bands';

type Difficulty = 'beginner' | 'intermediate' | 'advanced';

interface Exercise {
  id: string;
  name: string;
  muscleGroups: MuscleGroup[];
  equipment: Equipment;
  difficulty: Difficulty;
  description: string;
  steps: string[];
}

interface Workout {
  id: string;
  name: string;
  exercises: WorkoutExercise[];   // {exerciseId, sets, reps, restSeconds}
  createdAt: string;
}

interface WorkoutLog {
  id: string;
  workoutId: string;
  workoutName: string;
  date: string;
  startedAt: string;
  completedAt: string;
  exercises: CompletedExercise[];  // {exerciseId, sets: CompletedSet[]}
  durationMinutes: number;
}

interface ScheduledWorkout {
  id: string;
  workoutId: string;
  workoutName: string;
  date: string;   // YYYY-MM-DD
}
```

#### Локальное хранение (`data/storage.ts`)

AsyncStorage с тремя ключами:

| Ключ | Данные | Операции |
|---|---|---|
| `fit_app_workouts` | `Workout[]` | load, save, add, delete, update |
| `fit_app_workout_logs` | `WorkoutLog[]` | load, save, getByDate, delete |
| `fit_app_scheduled` | `ScheduledWorkout[]` | load, save, add, delete, getByDate |

#### API клиент (`data/api.ts`)

Обёртка над `fetch` с автоматической привязкой JWT-токена:

```
API_URL = http://192.168.0.103:3000/api  (мобильное)
API_URL = http://localhost:3000/api       (web)
```

Все функции — `async`, возвращают JSON ответа:

| Группа | Функции |
|---|---|
| **Токен** | `getToken`, `setToken`, `removeToken` |
| **Кэш юзера** | `getCachedUser`, `setCachedUser`, `removeCachedUser` |
| **Авторизация** | `apiRegister`, `apiLogin`, `apiLogout`, `apiGetMe`, `apiUpdateProfile`, `apiChangePassword` |
| **Тренировки** | `apiGetWorkouts`, `apiCreateWorkout`, `apiDeleteWorkout` |
| **Логи** | `apiSaveWorkoutLog`, `apiGetWorkoutLogs`, `apiGetStats` |
| **Расписание** | `apiGetScheduled`, `apiScheduleWorkout`, `apiDeleteScheduled` |
| **Упражнения** | `apiGetExercises`, `apiGetExercise` |

#### Контекст авторизации (`data/auth-context.tsx`)

React Context, оборачивающий всё приложение:

```
<AuthProvider>
  user          — объект пользователя или null
  loading       — загрузка при старте
  login(email, password)        → вызывает apiLogin
  register(email, password, name) → вызывает apiRegister
  logout()                      → очищает токен и кэш
  refreshUser()                 → перезагружает данные с сервера
</AuthProvider>
```

**При запуске приложения:**
1. Проверяет наличие токена в AsyncStorage
2. Загружает кэшированного пользователя (мгновенный показ)
3. Валидирует токен через `GET /api/auth/me`
4. Если токен невалиден → автоматический logout

---

### Тема и стили

Файл `constants/theme.ts` задаёт всю визуальную систему:

#### Цветовая палитра

| Токен | Light | Dark |
|---|---|---|
| `background` | `#F2F2F7` | `#0F0F1A` |
| `surface` | `rgba(255,255,255,0.8)` | `rgba(30,30,50,0.7)` |
| `tint` (основной) | `#6C63FF` | `#8B83FF` |
| `text` | `#1C1C1E` | `#F9FAFB` |
| `textSecondary` | `#8E8E93` | `#9CA3AF` |
| `success` | `#34C759` | `#34D399` |
| `danger` | `#FF3B30` | `#F87171` |
| `warning` | `#FF9500` | `#FBBF24` |
| `cardBackground` | `rgba(255,255,255,0.6)` | `rgba(30,30,50,0.6)` |

#### Отступы и радиусы

```
Spacing:  xs=4  sm=8  md=12  lg=16  xl=20  xxl=24  xxxl=32
Radius:   sm=8  md=12  lg=16  xl=20  full=999
```

#### Glassmorphism-стиль

Каждая карточка (`GlassCard`) использует:
- `expo-blur` BlurView с intensity 40
- Полупрозрачный фон (`cardBackground`)
- Тонкая рамка (`cardBorder`)
- Опциональная тень (`elevated`)
---
### Аутентификация
![система аунтификации](https://imgur.com/a/fH0qD9O)
**Токен** — JWT, подписан через `JWT_SECRET`, срок жизни 30 дней.
**Пароль** — хешируется bcrypt с 12 раундами.
Все защищённые эндпоинты проверяют заголовок `Authorization: Bearer <token>`.
---
## Backend (Node.js)

### Структура бэкенда

```
backend/src/
├── index.js              # Express сервер
│   ├── CORS (все origins)
│   ├── JSON body parser
│   ├── Request logger
│   └── Маршруты:
│       ├── /api/auth      → routes/auth.js
│       ├── /api/workouts  → routes/workouts.js
│       ├── /api/exercises → routes/exercises.js
│       └── /api/health    → { status: 'ok' }
│
├── db/
│   ├── pool.js           # pg Pool (host:port:db:user:pass)
│   ├── create.js         # SQL: DROP + CREATE таблицы
│   └── seed.js           # Вставка 20 упражнений
│
├── middleware/
│   └── auth.js           # generateToken() + authMiddleware
│
└── routes/
    ├── auth.js           # Register, Login, Me, Profile, Password
    ├── workouts.js       # CRUD тренировок + логи + стата + расписание
    └── exercises.js      # Список и детали упражнений из БД
```

### API эндпоинты

#### Аутентификация (`/api/auth`)

| Метод | Путь | Auth | Описание |
|---|---|---|---|
| `POST` | `/register` | — | Регистрация. Body: `{email, password, name}`. Возвращает `{user, token}` |
| `POST` | `/login` | — | Вход. Body: `{email, password}`. Возвращает `{user, token}` |
| `GET` | `/me` | JWT | Текущий пользователь |
| `PUT` | `/profile` | JWT | Обновить профиль. Body: `{name?, avatar_url?, height_cm?, weight_kg?, birth_date?, goal?}` |
| `PUT` | `/password` | JWT | Сменить пароль. Body: `{currentPassword, newPassword}` |

#### Тренировки (`/api/workouts`)

| Метод | Путь | Auth | Описание |
|---|---|---|---|
| `GET` | `/` | JWT | Все тренировки пользователя (с упражнениями через `json_agg`) |
| `POST` | `/` | JWT | Создать тренировку. Body: `{name, exercises: [{exerciseId, sets, reps, restSeconds}]}` |
| `DELETE` | `/:id` | JWT | Удалить тренировку |
| `POST` | `/logs` | JWT | Сохранить лог тренировки (транзакция: log → exercises → sets) |
| `GET` | `/logs` | JWT | Все логи (лимит 100) |
| `GET` | `/stats` | JWT | Агрегированная статистика (всего тренировок, минут, за неделю) |
| `GET` | `/scheduled` | JWT | Запланированные тренировки |
| `POST` | `/scheduled` | JWT | Запланировать тренировку на дату |
| `DELETE` | `/scheduled/:id` | JWT | Удалить запланированную |

#### Упражнения (`/api/exercises`)

| Метод | Путь | Auth | Описание |
|---|---|---|---|
| `GET` | `/` | — | Все упражнения. Query: `?muscle=chest&equipment=barbell&difficulty=beginner&search=жим` |
| `GET` | `/:id` | — | Одно упражнение с шагами выполнения |

#### Служебный

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/api/health` | `{ status: 'ok', timestamp: '...' }` |

---

### База данных PostgreSQL

#### ENUM типы

```sql
CREATE TYPE muscle_group AS ENUM (
  'chest', 'back', 'shoulders', 'biceps', 'triceps',
  'legs', 'glutes', 'abs', 'forearms', 'calves'
);

CREATE TYPE equipment_type AS ENUM (
  'barbell', 'dumbbell', 'cable', 'machine',
  'bodyweight', 'kettlebell', 'bands'
);

CREATE TYPE difficulty_level AS ENUM (
  'beginner', 'intermediate', 'advanced'
);
```

#### Схема таблиц
![ERD](https://imgur.com/a/nDKSfV6)
**10 таблиц**, все с CASCADE удалением, индексы на `user_id` и `date`.

### Middleware

**`auth.js`** — две функции:

```javascript
generateToken(userId)   // → JWT с payload {userId}, срок 30 дней
authMiddleware(req, res, next)  // → проверяет Bearer токен, ставит req.userId
```

---

## Landing Page

Маркетинговая страница в стиле приложения (тёмный glassmorphism).

### Секции

1. **Hero** — заголовок + макет телефона с placeholder'ом скриншота + плавающие статистические карточки
2. **Stats Banner** — 20+ упражнений, 10 групп мышц, 7 типов оборудования, 100% бесплатно
3. **Features** — 6 карточек возможностей (каталог, календарь, статистика, тренировки, таймер, прогресс)
4. **Screenshots** — 5 рамок-телефонов с placeholder'ами для скриншотов
5. **How It Works** — 3 шага (выберите → создайте → тренируйтесь)
6. **Demo** — интерактивный каталог упражнений + конструктор тренировки прямо в браузере
7. **CTA** — кнопки App Store / Google Play
8. **Footer**

### Файлы

| Файл | Строк | Содержимое |
|---|---|---|
| `index.html` | ~310 | Разметка всех секций |
| `style.css` | ~630 | CSS с переменными, совпадающими с темой приложения |
| `script.js` | ~250 | Данные 20 упражнений + логика демо (поиск, фильтр, builder) |

---

## Потоки данных

### Двойное хранилище

Приложение использует **два источника данных параллельно**:
![Описание работы](https://imgur.com/a/oxd7xDj)
**Текущее состояние:**

| Функция | Источник данных |
|---|---|
| Каталог упражнений | Клиентский массив (`data/exercises.ts`) |
| Создание/список тренировок | AsyncStorage |
| Выполнение и логирование | AsyncStorage |
| Планирование в календаре | AsyncStorage |
| Авторизация и профиль | Backend API |
| Статистика на экране профиля | Backend API (`/api/workouts/stats`) |

> **Примечание:** API-клиент (`data/api.ts`) имеет полный набор функций для тренировок/логов/расписания через бэкенд, но экраны пока используют локальное хранилище. Это позволяет приложению работать **оффлайн** с возможностью будущей миграции на серверное хранение.
### Жизненный цикл тренировки
```
1. Создание             2. Планирование           3. Выполнение
   workout/create           calendar.tsx               workout/run.tsx
   ┌────────────┐          ┌────────────┐             ┌────────────┐
   │ Выбрать    │          │ Выбрать    │             │ Подход     │
   │ упражнения │          │ день       │  ──────→    │ за         │
   │ + подходы  │          │ + тренировку│            │ подходом   │
   │ + повторы  │          └────────────┘             │ + таймер   │
   │ + отдых    │               │                     │ отдыха     │
   └────────────┘               │                     └────────────┘
        │                       ▼                          │
        ▼                  ScheduledWorkout                ▼
     Workout                (AsyncStorage)            WorkoutLog
   (AsyncStorage)                                    (AsyncStorage)
        │                                                  │
        └──────→ Главная ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┘
                index.tsx
             (дашборд, статы)
```
---
